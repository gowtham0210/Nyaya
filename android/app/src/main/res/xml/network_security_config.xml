<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain HTTP (not HTTPS) only to the local dev machine's loopback
  address as seen from the Android emulator, so the app can reach the
  NYAYA backend running on the developer's own computer during development.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
